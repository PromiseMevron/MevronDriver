dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
        // Map Box requires this
        jcenter()
    }
}
rootProject.name = "Mevron"
include ':app'
